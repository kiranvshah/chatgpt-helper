{
  "name": "chatgpt-helper",
  "displayName": "ChatGPT Helper",
  "description": "Make queries to OpenAI's ChatGPT from inside VS Code.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.73.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:chatgpt-helper.helloWorld",
    "onCommand:chatgpt-helper.askChatGPT"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "chatgpt-helper.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "chatgpt-helper.askChatGPT",
        "title": "Ask ChatGPT about your code"
      }
    ],
    "configuration": {
      "title": "ChatGPT Helper",
      "properties": {
        "chatgpt-helper.authentication.OpenaiEmail": {
          "type": "string",
          "format": "email",
          "description": "Your OpenAI email to authenticate requests to ChatGPT. This will not be sent or stored to anywhere except to openai.com. You are solely responsible for all data sent to OpenAI and the use of the service in this way."
        },
        "chatgpt-helper.authentication.OpenaiPassword": {
          "type": "string",
          "description": "Your OpenAI [assword to authenticate requests to ChatGPT. This will not be sent or stored to anywhere except to openai.com. You are solely responsible for all data sent to OpenAI and the use of the service in this way."
        },
        "chatgpt-helper.queries.queryText": {
          "type": "string",
          "default": "Explain what is wrong with the following code:",
          "description": "The default query to send to ChatGPT. Will be sent alongside (before) the code."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/puppeteer": "^7.0.4",
    "@types/vscode": "^1.73.0",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "@vscode/test-electron": "^2.2.0",
    "eslint": "^8.28.0",
    "glob": "^8.0.3",
    "mocha": "^10.1.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "puppeteer": "^19.3.0"
  }
}
